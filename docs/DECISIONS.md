# DECISIONS.md
> Locked decisions for this site. These are deliberate scope choices — do not expand, "improve," or add functionality beyond what's written here without flagging it and asking first.
> This document leads the code: if a decision changes, update this file in the same commit as the code that changes.

## Locked

### Freight types
Dry Van only. The Services page is built as a data-driven list (see ARCHITECTURE.md → Data model) so other trailer types can be added as a new entry later — but don't add Reefer/Flatbed copy or "growing fleet" language now.

### Fleet Map (was "Track a Load")
A public map of roughly where our trucks are right now, fed by Samsara and refreshed about once an hour (live updates aren't required). It's part of the company's own site, not a broker portal: no load lookup, no login, no account system — do not add auth to this page. Renamed from "Track a Load" and moved to `/fleet-map` on 2026-09-11. Show approximate positions with no load or driver details — exact positions of loaded or parked trucks are a cargo-theft and driver-privacy risk (how positions are coarsened is the backend teammate's call).

### Get a Quote
A US map next to a short form (requested 2026-09-11). The map shows only the lower 48 states and Washington, D.C. We don't run to Alaska or Hawaii, so they're left off instead of shown greyed out. Hovering a state lifts it and shows its name. The first click sets pickup (dark), the next sets delivery (orange), and a route line draws between them. Clicking a picked state clears it, and Swap and Clear buttons sit under the map. The map only picks the state: each stop also has a City or ZIP field, and typing a ZIP lights up its state. The state dropdowns do everything the map does, so phones and keyboard or screen-reader users can finish without the map. Fields: pickup (state, city/ZIP), delivery (state, city/ZIP), weight, what's being shipped, name, company (optional), email, phone. Every load goes in a dry van, so there's no trailer choice. Until the backend exists, the form checks fields in the browser and calls a stub in `lib/forms.ts`. It doesn't send anything yet.

### Applications (Drive For Us, Dispatcher, Mechanic/Office staff)
All roles use the same short 5–6 question qualification form. HR contacts the applicant directly afterward. No long-form online application, no CDL document upload, no e-signature in this phase.

The homepage offers three ways in (requested 2026-09-17): Driver, Dispatcher and Tire shop, as three flat cards where the photo band used to be (`components/home/ApplyRoutes.tsx`, copy in `lib/site.ts` → `applyRoutes`). They are three doors into the one form, not three applications. Driver goes to `/careers/drivers`; **Dispatcher and Tire shop both go to `/careers/staff`**, which is still a placeholder and doesn't separate the two roles — see Open.

### Pay transparency
No public per-mile pay rates. The in-app driver bonus tracker is the featured differentiator instead — don't publish rate figures on the Drive For Us page.

### Hero media
**Homepage** (requested 2026-09-10; the 3D scene was replaced by the video on 2026-09-11): a 6-second intro that plays by itself when the page opens. A 5-second video follows our truck down a desert highway; the picture fades to white, our logo appears in the middle of the screen, glides into the header's top-left corner, and the page shows through. (Until 2026-09-18 the logo first appeared over the one painted on the trailer and peeled off it; the owner asked for it to simply appear in the middle and move from there.) Until 2026-09-11 this was a 3D scene (three.js) telling the same story; the owner's produced clip replaced it, which this entry already allowed for, and three.js / React Three Fiber are no longer dependencies. The logo is still always exact: it's our own `logo.svg` laid over the video, not something the video has to get right. Nobody has to wait: scrolling, a tap, a scroll key or the Skip intro button jumps to the end in about half a second, and the header stays clickable the whole time. Skipped for reduced-motion visitors; if the video can't start within 3 seconds (slow network, autoplay turned off), the page just shows. Plays once per visit (requested 2026-09-10): coming back to the homepage later in the same visit goes straight to the content; a reload or a new visit plays it again. 
**Homepage hero — forest drone loop, 2026-09-23** (the owner liked the United Carriers hero on Mobbin and asked for something similar). Supersedes the cinematic photo hero described next. A top-down drone loop of one truck on a straight two-lane forest highway fills the screen (`public/videos/home-hero-forest.mp4`, poster `public/images/home-hero-forest.jpg`). **The footage is AI-generated** (Grok, generated by the owner after four desert attempts and three forest ones); it's a stand-in, like the photos before it, until real drone footage exists. The current clip (swapped in 2026-09-23, replacing a first forest take that had to be cut short before a power line) starts and ends on an empty road: the truck enters at the bottom and leaves at the top. It was slowed to 0.8× with frame blending so it stays at 24 fps, and its last 0.6 s dissolves into its first so it loops without a jump — during that fade only the truck's rear at the very top edge and its nose at the bottom edge overlap (6.75 s, 3.6 MB, no audio). The road runs down the middle, so the words sit on the forest either side: **one oversized decorative word, "DRIVE.", along the bottom left** — `aria-hidden`, in Archivo at its widest (wdth 125), the only place that face is used — and **the approved line as the h1 on the right**, with Apply to drive under it. From `lg` that block sits exactly in the header's CTA column — left edge on Get a Quote, right edge on Apply To Drive, with the hero button spanning the same width (owner, 2026-09-23: it looked off-grid at 60% of the width). The h1's words start dim and light up one by one as the intro hands over. On scroll the footage zooms in slightly and darkens, and DRIVE. drifts up slower than the page. The video plays only while on screen and not at all under reduced motion (the poster shows instead). **The draft lede ("Dispatch that answers on the first ring…") left the hero** to keep it to one line and one button; `homeLede` is still in `lib/site.ts` if it's wanted elsewhere. "DRIVE." is a copy choice made with the owner's go-ahead in chat; change it in `HomeHero.tsx`. The whole hero is marked `data-header-theme="dark"`, so the header uses its light logo over all of it.

**Homepage hero — cinematic rebuild, 2026-09-23** (after a Mobbin review against Waabi and Aurora; the owner approved the direction). Supersedes the 4a layout below. The desert truck photo fills the screen at full strength — **no white wash**. A dark scrim rises from the bottom (plus one from the left from `lg`, faded out toward the top), and the top of the frame stays clear sky so the header sits over it unchanged. The h1 is the same approved line, now **one colour (white)** at up to 100px, three lines on desktop — the grey two-tone tail was dropped because it lost contrast over the white truck. Under a hairline: the draft lede and **one button, Apply to drive** (white, orange on hover — the `light` Button variant). Get a quote left the hero; it stays in the header. The hero's two numbers (10+ years, 99% on-time) moved into the numbers band, which now has five. The scrimmed lower 45% of the hero is marked `data-header-theme="dark"` so the header shows its light logo while that part passes under it.

**Homepage hero — rebuilt 2026-09-21 from the owner's "4a" reference** (`files/hero-4a-standalone.html` in their `Itrucking SOL Web Page` folder). The truck photo (`public/images/home-hero-desert.jpg`, the front-quarter desert shot at full resolution, taken from the reference) now fills the whole screen, with a white wash from the left (top-to-bottom below `lg`) so the words read over it. Up top: the same fixed h1 (the reference's "Driver-first carrier since 2009" label pill was built, then removed the same day at the owner's request; the company was founded in **2008**, not 2009), and Apply to drive + Get a quote as buttons. Along the bottom: the lede, and two numbers split off by a hairline — **10+ years in business and 99% on-time delivery**, which the owner asked for in place of the reference's "1,200 drivers" and "97% on-time". The site header, logo and nav stay exactly as they were; the reference's own nav bar was not used. The photo eases in from a slight zoom and the words rise in (`animate-hero-settle` / `animate-hero-rise` in `app/globals.css`), both off under reduced motion. On phones the full-bleed portrait crop shows the cab, not the whole rig, as in the reference. The numbers band straight below used to open on "10+ years in business" too, so it was dropped and the band now has three numbers: miles, loads and states (2026-09-21; a fleet-size stat, about 60 trucks and trailers, was tried and removed the same day). The fleet is nearly all brand new, so the Ship with us equipment line says "Nearly all new equipment", not "New equipment only". The 4a rebuild replaces the split described next, and the photo swaps and 4:3 panel noted under Audience split no longer apply to the hero; `public/images/safety-truck-front.jpg` is now unused.

**Previous hero (2026-09-18 to 2026-09-21)** (built 2026-09-18 from the owner's "2a" design reference, `hero-2a-standalone.html` in their `Video/` folder, after asking for a minimalist, Samsung-like opening with a lighter feel): a light split. Left, on white and set low in the screen: the fixed h1 "Where you're known by your name, not your truck number." (the tail in grey), one lede line, a dark "Apply to drive" button and a quiet underlined "Get a quote" link. Right: our truck climbing a mountain highway through pine forest at dawn, in a full-height panel (52% of the width), lifted slightly lighter and less saturated so it sits with the white page. The intro ends in the Nevada desert on I-80 and the hero picks the same road up in the Sierra; the white fade between them hides the change of scene. **The site header stays exactly as it is over the hero** — the owner said not to move the logo, nav or top buttons, so the reference's own nav bar was not used. Things that differ from the reference on purpose: the reference's "I-80, Sierra Nevada" place line is gone (owner, 2026-09-19; it also labelled an AI-generated photo as a real place), the headline and lede are larger, "Get a quote" is ink with an underline instead of orange text (orange at that size fails contrast), and the page stays white rather than the reference's off-white `#f7f6f3`, so the hero doesn't end in a seam against the white sections below. Apply to drive is the main button here and Get a quote the link — the reverse of the header — because the headline speaks to drivers; Get a Quote stays one click away in the header. The split only starts at `xl` (1280px): at 1024px the panel is too narrow for the rig and cut the trailer's logo, so below `xl` it stacks — photo, then the words, with the photo's height following the screen width so the whole rig always fits. The photo is AI-generated from the owner's own branded version (`image-1789773471240.png` in their `generated-images/` folder). A dark full-screen version of the same photo with white text over it was built first and replaced the same day by this one. **The rolling slogans are no longer the h1**: the owner asked for "one fixed line for hero" with the changing slogans "under her with smaller shrift", so the other three slogans roll in a smaller line directly under the hero, above the apply cards. **The lede is unapproved copy** ("Dispatch that answers on the first ring. Routes you agreed to. Home when we said.") — three promises from the reference, needing sign-off like the slogans, and it overlaps two of them (the dispatcher and home-time lines).

Before 2026-09-18 the top of the page under the intro was the headline, and the full-width photo band sits below it (moved 2026-09-17 at the owner's request — the page now opens on the words, not the photo). The band is a fixed 500px tall (set 2026-09-17; it was a wide 5:2 strip from 2026-09-11, capped so the headline showed on the first screen). It is shown straight, with no overlay; repeat visits and skipped intros show it straight away (requested 2026-09-10). It carried a warm sunset color grade and film grain until 2026-09-17, when the owner said the photo looked dated — the grade, the grain and the extra saturation were removed and the `sunset-grade` / `film-grain` utilities deleted. The composition is still the open question: a clean truck shot three-quarter-front in golden hour is the stock trucking photo, so a darker, tighter, cooler replacement is wanted (and would then want the intro video's sunset ending re-checked against it). That photo is a temporary AI-generated placeholder (a white 2026 Volvo VNL 860 with our logo on the trailer and cab, on an open desert highway at sunset — swapped in from the bridge shot on 2026-09-17 at the owner's request) — B-roll video will replace it in the same spot.

**The homepage photo band was replaced by the three apply cards on 2026-09-17** (requested: "add like 3 flat cards to the main screen"). All three cards use the owner's own photos, and all three are people: a driver in front of his truck, a dispatcher at his screens, and a tire tech in the shop (replaced an empty shop interior on 2026-09-18, which was the only card without a person in it). The truck photo briefly became the driver card and was replaced by the driver portrait the same day ("for driver use box use pictures from folder driver"), so **`home-hero-desert.jpg` is no longer used anywhere** — it's kept for whenever the band or another hero comes back. The cards are near-square from `md` up, so a 3:2 photo loses a third of its width there; the driver stands left of centre and needs `object-position: 20%` (`applyRoutes[].image.position` in `lib/site.ts`) or a centred crop cuts him in half. The tire-tech photo is the reverse case — nearly square, so it's the phones' 3:2 crop that bites, and it's biased up (`50% 30%`) to keep headroom above his cap. That photo also carries Gemini's small sparkle mark in its bottom-right corner, visible on the square desktop crop; the owner hasn't decided whether to keep it. `HeroMedia` is kept for other pages' heroes but is no longer used anywhere — everything above about the band's height, grade and grain describes what to restore if it comes back. The open question about the truck photo's composition still stands, and now applies to the driver card.

The intro video is the AI-generated clip the owner supplied (`Video/` folder on their Desktop, kept out of the repo). It ships re-encoded for the web: no audio track, fast-start, 1080p (~3.2 MB) with a 720p copy (~1.6 MB) for phones and the first frame as the poster. Since the logo no longer sits on the trailer, a different clip only needs to end around 5 seconds — or the timings in `components/intro/timeline.ts` moved to match.

**Other pages** (e.g. Drive For Us): the hero must support photo or video — video may be produced and dropped in later, but is not required to launch.

### Company story on the homepage
No full About section on the homepage (decided 2026-09-10). A single short story band sits after the numbers — headline, 2–3 sentences, and three milestones from `md` up (hidden on phones to keep the band short) — and the whole band links to `/about` for the full story. **Open:** the whole-band click target was fine when this was an inset card; now that the band runs edge to edge it is a very large invisible target, and it may be worth cutting back to just the "Read our full story" link. Copy lives in `lib/story.ts` and is a draft for now (see Open).

### Trust badges
DOT/MC numbers are confirmed and should be shown now — the actual numbers still need to be supplied. Other certifications (TSA/TWIC/hazmat, etc.) are not yet available — don't invent or imply certifications that aren't confirmed.

Homepage numbers band (figures given by the owner 2026-09-10; years in business moved to the hero on 2026-09-21 and back on 2026-09-23 with 99% on-time delivery): 10+ years in business, 32M+ miles driven, 125,000+ loads completed, 48 states, 99% on-time delivery — one row of five from `lg`. (Miles was first "about 1M a year"; replaced with the 32M+ total the same day.) The numbers fill up from zero when the band scrolls into view. Add a figure only once it's confirmed.

### Navigation & clutter
Minimal, confident nav — closer to Sierra Expediting's restraint than a dense multi-menu site. No mega-menus, no duplicate link lists, no multiple phone numbers in the header. If a new page or CTA is requested, check it fits the existing header/footer structure before adding a nav item.

Header behavior (Lucid-inspired): stays in view while you scroll, so Apply To Drive and Get a Quote are always one click away (changed 2026-09-10 — it used to hide on scroll down on every screen). On phones (under 640px), where the bar only shows the logo and menu button, it still hides on scroll down and returns on scroll up. During the homepage intro the nav pill and buttons stay visible but dimmed over the scene and come to full on hover (requested 2026-09-10); the logo appears only once it lands from the intro. Look (requested 2026-09-16): no full-width bar — the logo sits alone in the top-left corner, the nav links sit in a centered frosted-glass pill, and Get a Quote and Apply To Drive are their own pills on the right, outside the nav pill; on phones the menu button is a round glass pill. The glass firms up a little once you scroll. "Careers" opens a small glass panel with two links that drops from under the nav pill — a dropdown, not a mega-menu. Careers links: Drive For Us → `/careers/drivers`, Dispatch, Shop & Office → `/careers/staff`. Nav order (News added 2026-09-11): Home · Services · About · News · Careers ▾ · Fleet Map. Between 1024 and 1280px the two CTAs size to their labels and nav items are tighter, so the row fits (it ran off the right edge before 2026-09-23); the matched fixed width starts at `xl`. Over dark bands (any element marked `data-header-theme="dark"` — the story band and the whole hero) the logo swaps to `logo-light.svg`, Get a Quote turns solid white and Apply To Drive gains a light ring.

### Homepage section look
Bands, not cards (requested 2026-09-17: "i want to go away from these cards"). The homepage used to be four `rounded-[2rem]` boxes inset in white — the two audience panels, the numbers band and the story card — all with the same corner radius, so nothing in the stack read as more important than anything else. Sections now change background at full width instead: the numbers sit as plain type under a single hairline with no box, and the story is the page's one edge-to-edge dark passage. The light/dark audience split described here was removed on 2026-09-18 (see Audience split below); its replacement, the Ship with us band, holds to the same rule — the photo runs to the screen edge rather than sitting in a box. Nothing floats, and border-radius is reserved for things you can click.

Cards came back on 2026-09-17, but flat (requested: "add like 3 flat cards"). This is a narrowing of "bands, not cards", not a reversal: the three apply cards have no radius, no shadow and no panel behind them — a photo, a role and a line, sitting directly on the page. The thing the owner rejected was boxes floating on a background, and nothing here floats. Keep it that way: if these ever grow a border, a corner radius or a shadow, they become the thing that was removed.

Tracked-out ALL-CAPS eyebrows were dropped at the same time. The page had six of them; they are now sentence-case labels in the same small semibold size. Keep the sentence-case labels, not the all-caps treatment. ("Ship with us" / "Drive for us" was described here as the fork the whole homepage exists to offer; that stopped being true on 2026-09-18, when the fork moved up the page — see Audience split.)

### Audience split
Removed on 2026-09-18 ("remove drive for us"). The homepage used to fork shipper vs driver in one flush light/dark band near the top. The three apply cards now carry every driver route, so the dark "Drive for us" half was the driver's third invitation in two screens, after the header button and the cards. What is left is a single shipper band, `components/home/ShipWithUs.tsx`: label, heading, one line, Get a Quote, with the logo truck photo on the left, angled right edge, running to the screen edge from `lg`. **Sides swapped 2026-09-19** (owner): the hero's photo is on the right, so Ship with us puts its photo left and the safety band puts its photo right, and the page alternates right, left, right down its length. The two bands' photos were swapped too, so each truck faces toward its text: the front-quarter shot now sits in Ship with us, the side-on shot in the safety band. **Hero and Ship with us photos swapped 2026-09-21** (owner): the front-quarter desert truck is now the hero photo, and the Sierra shot sits in Ship with us. The owner then asked to see the whole truck, not only the tractor, so from `xl` the hero photo is no longer a full-height panel: it is a 4:3 box on the right, running to the screen edge and centred in the screen's height, with its height capped to clear the header. The hero description above still names the Sierra shot and should be read with that in mind. The same day, Ship with us gained a line under its paragraph: new equipment only, 2025–2026 Volvo trucks and brand-new 2025–2026 trailers (owner's claim; update the years as the fleet turns over). `AudienceSplit` and `AudiencePanel` were deleted rather than left unused — git history has them if the split is ever wanted back.

The fork itself did not disappear, it moved: drivers are served by the apply cards above, shippers by this band. Keep it that way — do not re-add a driver CTA here.

### Safety band
Added on 2026-09-18 ("the security systems we use"). A homepage band on how the freight is looked after: GPS on trucks and trailers and dash cameras (Samsara), maintenance history (Fleetio). It follows Ship with us because it answers the shipper's next question, with the numbers band between them as the divider — hairlines only, no background change (owner, 2026-09-18), and it is not the About section — "No full About section on the homepage" still holds; StoryTeaser is the link to the company story. **Order changed 2026-09-21** (owner: "slogans are for drivers"): Ship with us now sits straight under the hero, and the driver slogans and apply cards follow it as one driver block, and the numbers band moved up to sit straight under the hero, and the safety band then moved up to follow Ship with us directly, so both shipper bands sit together before the driver slogans and apply cards (also 2026-09-21).

Three rules for its copy:
- **Say we track the equipment, never that a shipper can watch their load.** The Fleet Map is approximate on purpose (cargo theft, driver privacy), so no "live tracking" promise here.
- **Cameras record the road.** The page above it is recruiting drivers; describe the cameras as protection, not as watching the driver.
- **Maintenance history is "on record", not "you can see it".** Customers have no access to Fleetio.
- **No vendor names.** The copy lists what we have, not whose system it is (owner, 2026-09-18). Confirmed by the owner the same day: every tractor and every trailer has a tracker, and the dash cameras face the road only — so "road-facing" is a fact, not a hedge. Don't add a driver-facing claim.

**No Samsara marketing video.** The owner wanted the two clips in `Video/samsara video` (Centralized Visibility, Helicopter View). Both were downloaded from samsara.com: they are Samsara's copyrighted marketing assets, and they show Samsara's demo data ("Vehicle 142", driver "Tyler Smith", a Sunnyvale address) that visitors would read as our fleet — one of them shows an exact live position, which contradicts the Fleet Map rule. The band uses a still photo instead. Video can replace it if Samsara approves its clips in writing, or with footage we own (a screen recording of our own dashboard with truck numbers and names blurred, or road-facing dash cam footage).

**Hover clips, placeholders only** (requested 2026-09-19). Each of the three rows now swaps the photo for its own muted clip while it is hovered, focused or tapped; leaving the list brings the photo back. The owner will send our own footage, and until then the clips are stand-ins: GPS uses the Samsara "Helicopter View" clip despite the rule above, by the owner's choice, **as a local placeholder only — it must be replaced before the site goes live**; dash cameras and maintenance use free Pexels stock (dash cam: 5382495, real dash cam footage on a US interstate; maintenance: 6685045 by Gustavo Fring; Pexels licence, no credit required). The files are `public/videos/safety-*-placeholder.mp4`, set per row in `lib/site.ts` → `safetySystems`. The same treatment is planned for Ship with us next.

### Stack
Next.js (App Router) + Tailwind CSS + TypeScript. Next.js chosen for consistency with the company's existing fleet dashboard project, and for SEO (server-rendered/static pages matter for a site trying to be found by shippers and drivers) and room to grow (Route Handlers can serve the form backend later without a separate service). App Router chosen over Pages Router as the actively developed path.

Everything is built as React components organized as a small library (see NAVIGATION.md → Component library). Animation: motion + Lenis smooth scrolling. 3D: three.js via React Three Fiber, homepage intro only.

### Team split
This repo's owner builds the **frontend only**: pages, components, layout, styling, client-side form validation, and the map UI. Another team member owns the **backend**: form submission handling, the Samsara integration (truck positions for the Fleet Map), and any API keys or data storage. Frontend code talks to the backend through a small agreed contract (endpoint + request/response shape); until that exists, use typed stubs in `lib/` rather than building backend logic here.

### Brand orange
The site's orange is `#FF3000`, matching the logo icon exactly. The earlier palette value `#FA5F1A` is retired — don't reintroduce it. See ARCHITECTURE.md → Brand / design tokens for contrast rules.

**Buttons are black, not orange** (owner, 2026-09-19). Every button on the site is solid black (`primary`) or outlined in black (`outline`); the orange Apply To Drive and the frosted-glass Get a Quote were replaced, and the header's current-page marker went from a white pill to a black one that glides between items. In the header and the hero, Get a Quote and Apply To Drive are a matched pair: same width, same height, outline beside solid. Apply To Drive turns brand orange on hover (the `apply` variant); Get a Quote keeps its black hover. Orange otherwise stays for the logo and small accents.

## Open — not yet decided

- **Form backend**: how Quote / Careers form submissions get handled (email, Supabase, Next.js Route Handler to a CRM, other) is undecided.
- **Additional trust badges**: beyond DOT/MC, TBD.
- **Safety band copy**: draft wording; the facts are confirmed (see Safety band). Still open: whether DOT/MC numbers belong in this band once supplied.
- **News content**: who writes posts and where they live (files in the repo or a CMS) isn't decided, so `/news` is a placeholder for now.
- **Font**: Manrope is a provisional pick (free, close to the premium-but-light feel of the design references). Swap if a brand font exists — one line in `app/layout.tsx`.
- **Homepage copy**: the headline and the two panel texts are drafts. The owner supplied the driver slogan "Where you're known by your name, not your truck number." on 2026-09-17 and asked for the header to read as more driver-respect oriented, with a subtle call to action. It sits under the headline with a quiet text link ("Five questions to apply, then HR calls you back") rather than a third button, since the two loud CTAs are a screen below in the audience split. Two things to settle: the owner's example wording "sign now" isn't used, because Applications locks this phase to a short qualification form with no e-signature and a promise to sign would misdescribe it; and the homepage header now leans to drivers while shippers are served by the split below, so if shippers should keep equal billing at the top, the slogan may belong on `/careers/drivers` instead once that page is built.
- **Rolling slogans** (the h1 from 2026-09-17 to 2026-09-18; now a smaller line under the photo hero, which has the fixed h1 — so the SEO point below is settled in the owner's line's favour, and the length-matching only applies to the three remaining slogans): on 2026-09-17 the owner asked the slogan to roll over into a different one, then asked for the fixed headline ("Freight that moves. A team that drives it.") to be removed and the hero headline itself to swap slogans. So the homepage no longer has a static h1 — the rolling slogan is it. Built as an odometer roll (`components/ui/RotatingSlogan.tsx`, list in `lib/site.ts` → `driverSlogans`). Three of the four lines are unapproved claims, not descriptions, and need sign-off before launch: the dispatcher promise ("knows your route and picks up the phone"), the live bonus line, and "home time you ask for, not hope for". Only the owner's own line is approved, and it stays first because it's what screen readers and crawlers get. Also open: WCAG 2.2.2 asks for a way to stop auto-updating content that runs past five seconds. The roll pauses on hover, on keyboard focus and in a background tab, and reduced-motion visitors see a single static slogan — but there's no visible pause control. Add one, or cap the rotation at a few passes and stop, if this needs to be airtight. Two more things this raised, both needing the owner's call: **the h1 is now a driver-recruitment line, not a description of the business** — a crawler and a screen reader see only "Where you're known by your name, not your truck number.", which says nothing about dry van truckload shipping, so homepage search relevance for shipper terms now rests entirely on the `<title>` and body copy; and the slogans have to stay within a few characters of each other, because the band is sized to the tallest and a short one would otherwise sit above a blank line a full headline row high. The four current lines are tuned to within 4% of each other — re-check the line counts across widths after any copy edit. The owner asked on 2026-09-17 to keep the old headline's two-tone, so each slogan is a `{ lead, tail }` pair set in ink over grey, on its own line from `md` up and flowing inline below that. Holding both halves on one line each caps the headline at 72px rather than the 84px the fixed headline used — its lines were much shorter. Shorter slogans (roughly 24 characters a half) would buy the 84px back.
- **Company history**: the story on the homepage card and the About page (`lib/story.ts`) is made-up placeholder copy, approved as a stand-in on 2026-09-10. Only the Citrus Heights, CA base and the for-hire interstate authority come from the FMCSA SAFER record. Replace it once the real history is written.
- **Seasons in the intro**: discussed (winter → spring → summer → autumn, ending at sunset) but not built. Now that the intro is a video, this is a footage question, not a code one.
- **Three apply cards, two destinations**: the homepage now names Driver, Dispatcher and Tire shop, but `/careers/staff` is one placeholder covering dispatch, shop and office. Two of the three cards land on the same page, which will read as a dead end once that page is real. Either `/careers/staff` splits the roles on arrival (an anchor or a preselected role in the form), or it gets written to answer all three. Also note the homepage now says "Tire shop" while the Careers dropdown says "Dispatch, Shop & Office" and DECISIONS elsewhere says "Mechanic/Office" — three names for overlapping things, and they should be settled together with the dropdown wording below.
- **Apply card copy**: the three one-liners are drafts. "Class A, dry van, and the bonus tracker in your app" is safe (both facts are already decided), but "Plan the loads and keep our drivers moving" and "Mount, balance and road service, in our own shop" describe jobs nobody has confirmed the shape of — in particular whether the tire shop is our own or a partner's, which that line asserts. Needs the owner's sign-off.
- **Careers dropdown wording**: "Dispatch, Shop & Office" is a provisional label.
- **Footer extras**: Privacy Policy, Terms of Service and Driver FAQ pages, plus contact info and socials, aren't defined yet — so they're not in the footer.
